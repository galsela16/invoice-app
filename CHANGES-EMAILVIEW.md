# צפייה בתוכן המייל בתוך האפליקציה (כשאין קובץ) — v0.16.0 → v0.17.0

נבדק מול build אמיתי — עובר נקי. אין תלות npm חדשה.

## מה השתנה
- כשלרשומה אין קובץ מצורף, החלון עכשיו טוען ומציג את **תוכן המייל עצמו**
  בתוך האפליקציה (ולא רק כפתור ל-Gmail).
- התצוגה היא ב-iframe מבודד (sandbox) — בטוח: סקריפטים לא רצים.
- כפתור "פתח ב-Gmail" נשאר כאפשרות משנית בכותרת (וגם אם הטעינה נכשלה).
- הכותרת מציינת "מתוך גוף המייל" כשאין קובץ.

## קבצים בזיפ (העלה לאותם נתיבים)
1. src/lib/gmail.ts           — עודכן (fetchMessageHtml: מחזיר את גוף המייל כ-HTML).
2. src/components/InvoiceModal.tsx — עודכן (מציג קובץ אם יש, אחרת את תוכן המייל).

## הערה
תוכן המייל נטען מ-Gmail ברגע הפתיחה. אם ההרשאה פגה — תופיע שגיאה + כפתור Gmail.
