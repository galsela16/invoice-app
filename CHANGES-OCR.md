# שילוב חילוץ סכום מ־PDF — v0.5.0 → v0.6.0

נבדק מול build אמיתי (tsc -b && vite build) עם הקונפיג שלך — עובר נקי.

## קבצים בזיפ (להעלאה ל-GitHub, דורסים/מוסיפים לאותו נתיב)
1. src/lib/pdfAmount.ts   — חדש. חילוץ טקסט מ-PDF + זיהוי הסכום (מקבל Uint8Array).
2. src/lib/gmail.ts       — עודכן. הוספת enrichAmountsFromPdf אחרי messages.map(toInvoice),
                            עם import() דינמי כדי ש-pdf.js לא ייכנס ל-bundle הראשי.

## צעדים
1. package.json → הוסף ל-dependencies:
       "pdfjs-dist": "^6.2.108"
2. העלה את שני הקבצים מהזיפ.
3. מחק מהריפו: vite.config.js  ו-  vite.config.d.ts  (משאירים רק vite.config.ts).
   הם פלט קומפילציה שנדבק בטעות; Vite בוחר את ה-.js לפני ה-.ts ומתעלם מ-.ts.
4. Commit → Vercel בונה → בדוק על 2-3 חשבוניות אמיתיות עם PDF מצורף.

## מה זה עושה
- לכל חשבונית עם PDF מצורף: מוריד את הקובץ, מחלץ טקסט, ומזהה את הסכום
  (סה"כ לתשלום / כולל מע"מ / ₪ ...). מעדכן את inv.amount רק אם נמצא סכום > 0.
- אם אין PDF או שהחילוץ נכשל — נשאר הניחוש מהנושא (כמו היום).

## אימות bundle (מדוע import דינמי)
- import רגיל:  index ראשי 739KB (pdf.js בפנים)
- import דינמי: index ראשי 260KB, pdf.js כ-chunk נפרד שנטען רק במשיכת חשבוניות

## מגבלות / הבא בתור
- משיכה מורידה ומפענחת עד 25 PDF — קצת יותר איטי. אפשר להגביל מקביליות בהמשך.
- PDF סרוק (תמונה בלבד) לא ייחלץ — צריך OCR (Tesseract.js עברית), שכבה 2.
- לדיוק גבוה יותר: שליחת הטקסט שחולץ ל-AI דרך פונקציית Vercel, שכבה 3.
