# פתיחת כל הרשומות (גם בלי קובץ מצורף) — v0.14.0 → v0.15.0

נבדק מול build אמיתי — עובר נקי. אין תלות npm חדשה.

## הרקע
רשומות שלא נפתחו = אין להן קובץ מצורף (הסכום זוהה מגוף המייל, לא מ-PDF),
אז לא היה מה להציג בחלון.

## מה תוקן
- כל שורה עכשיו לחיצה.
- כשיש קובץ מצורף: נפתח כרגיל (PDF/תמונה + הורדה).
- כשאין קובץ: החלון מציג הודעה + כפתור "פתח ב-Gmail" שמוביל למייל המקורי.
- גם אם טעינת קובץ נכשלה — מופיע כפתור "פתח ב-Gmail" כגיבוי.

## קבצים בזיפ (העלה לאותם נתיבים)
1. src/components/InvoiceList.tsx  — עודכן (כל שורה לחיצה).
2. src/components/InvoiceModal.tsx — עודכן (פרטים + קישור ל-Gmail כשאין קובץ).

## הערה
קישור ה-Gmail בנוי ממזהה ההודעה. אם במקרה נדיר הוא לא נוחת בדיוק על המייל,
הוא ייפתח בתיבה — תגיד לי ואכוונן את פורמט הקישור.
