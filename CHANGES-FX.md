# המרת מטבע זר לשקלים — v0.7.0 → v0.8.0

נבדק מול build אמיתי (tsc -b && vite build) — עובר נקי.

## מה זה עושה
- הפרסר מזהה עכשיו גם את המטבע (₪/€/$/£ · EUR/USD/ILS/GBP).
- חשבונית במטבע זר מומרת לשקלים לפי שער ECB **בתאריך החשבונית**
  (מקור: Frankfurter — חינמי, בלי מפתח API, ישירות מהדפדפן).
- נשמר גם הסכום המקורי; בתצוגה מופיע ₪X עם המקור מתחת/לצד (למשל: ₪63.20 · €16.00).
- אם שער החליפין לא זמין (או כשל רשת) — נשאר הסכום המקורי, מסומן לבדיקה.

## קבצים בזיפ (העלה ל-GitHub לאותם נתיבים)
1. src/lib/pdfAmount.ts    — עודכן: מחזיר גם currency.
2. src/lib/fx.ts           — חדש: convertToIls לפי Frankfurter + cache.
3. src/lib/gmail.ts        — עודכן: המרה לשקלים אחרי חילוץ הסכום.
4. src/types.ts            — עודכן: currency / originalAmount / fxRate.
5. src/utils/format.ts     — עודכן: formatCurrencyCode (הצגת מטבע מקורי).
6. src/components/InvoiceList.tsx  — עודכן: מציג את הסכום המקורי מתחת ל-₪.
7. src/components/InvoiceModal.tsx — עודכן: מציג את המקור בכותרת.

## אימות
€16 @2025-07-08 → ₪63.20 (שער 3.95) | $100 → ₪368 | ₪590 → 590 (בלי המרה) | cache מונע קריאה חוזרת.

## הערות
- שער ECB מתפרסם בימי עסקים; לתאריך ללא שער ה-API מחזיר את הקרוב הקודם.
- לא מוסיף תלות ב-npm — fetch ישיר מהדפדפן (אין מפתח).
- הבא בתור אפשרי: הצגת שער ההמרה (fxRate כבר נשמר) לצורך ביקורת רו"ח.
