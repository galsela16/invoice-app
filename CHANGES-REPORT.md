# דוח Excel (חודש/שנה) + הסרת סטטוס — v0.12.0 → v0.13.0

נבדק מול build אמיתי (tsc -b && vite build) — עובר נקי.

## 1) דוח Excel
- שני כפתורים חדשים תחת "דוח Excel": לפי חודש / לפי שנה.
- הדוח (.xlsx) כולל שני גיליונות:
  - "סיכום": שורה לכל תקופה (חודש/שנה) עם מספר חשבוניות וסה"כ ₪, ושורת סה"כ הכל.
  - "פירוט": תקופה, תאריך, ספק, מקור, סכום (₪), מטבע, סכום מקורי.
- אינו מוריד קבצים מ-Gmail — מיידי. מתעלם מחשבוניות שהוסתרו.

## 2) הסרת סטטוס (שולם/לא שולם/לבדיקה)
- הוסר תג הסטטוס מהרשימה, סינון הסטטוס, ופירוק הסטטוס בסיכום.
- הסיכום מציג עכשיו סה"כ + מספר חשבוניות בלבד.
- כפתור "הסתר" (✕) נשאר בשורה.

## דרוש: הוספת תלות
package.json כולל עכשיו "xlsx": "^0.18.5". ה-ZIP כולל את package.json המעודכן.
Vercel יתקין אוטומטית ב-build. (xlsx נטען דינמית — לא מכביד על הטעינה הראשונית.)

## קבצים בזיפ (העלה לאותם נתיבים)
1. package.json                  — עודכן (תלות xlsx).
2. src/lib/exportReport.ts       — חדש (דוח Excel).
3. src/types.ts                  — עודכן (MonthlySummary: total+count).
4. src/lib/selectors.ts          — עודכן (בלי status).
5. src/App.tsx                   — עודכן (handler ל-Excel + ייצוא ללא מוסתרים).
6. src/components/SummaryPanel.tsx — עודכן (בלי פירוק סטטוס).
7. src/components/Filters.tsx      — עודכן (בלי סינון סטטוס).
8. src/components/InvoiceList.tsx  — עודכן (בלי עמודת סטטוס).
9. src/components/GmailConnect.tsx — עודכן (כפתורי Excel).

## אימות
לפי חודש: 2025-01=944, 2025-07=63.2, 2026-02=1200, סה"כ 2207.2 | לפי שנה: 2025=1007.2, 2026=1200.
