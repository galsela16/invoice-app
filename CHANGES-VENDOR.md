# פעולות לפי ספק: הסתרת הכל + הורדת כל הקבצים — v0.17.0 → v0.18.0

נבדק מול build אמיתי — עובר נקי. אין תלות npm חדשה.

## מה חדש
לחיצה על הכפתור בשורה (⋯, במקום ה-✕) פותחת חלון פעולות עם שם הספק:
1. **הסתר רשומה זו** — כמו קודם.
2. **הסתר הכל מ-<ספק>** — מסתיר את כל הרשומות מאותו ספק, ונשמר לצמיתות
   (גם רשומות עתידיות מאותו ספק לא יוצגו).
3. **הורד את כל הקבצים מ-<ספק>** — ZIP אחד עם כל הקבצים המצורפים של אותו ספק.

## הערות
- "הסתר הכל מ-<ספק>" מזהה לפי שם הספק כפי שמופיע ברשומה. אם ספק מופיע בשתי
  צורות שם שונות, אפשר להסתיר כל אחת בנפרד.
- אם לספק אין קבצים מצורפים (למשל Apple ששולח בגוף המייל) — ההורדה תציג
  "אין קבצים מצורפים מ-<ספק>".

## קבצים בזיפ (העלה לאותם נתיבים)
1. src/lib/dismissed.ts          — עודכן (רשימת ספקים מוסתרים).
2. src/lib/exportZip.ts          — עודכן (exportVendorZip).
3. src/components/ActionSheet.tsx — חדש (חלון הפעולות).
4. src/components/InvoiceList.tsx — עודכן (⋯ פותח פעולות).
5. src/App.tsx                   — עודכן (hideVendor + handleDownloadVendor).
