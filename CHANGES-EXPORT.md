# ייצוא לרו"ח לפי חודש או לפי שנה — v0.11.0 → v0.12.0

נבדק מול build אמיתי — עובר נקי.

## מה השתנה
- כפתור הייצוא הפך לשניים: "לפי חודש" ו-"לפי שנה".
  - לפי חודש: תיקייה לכל חודש (2025-07, 2025-08 ...) — כמו קודם.
  - לפי שנה:  תיקייה לכל שנה (2025, 2026), כל הקבצים של השנה בתוכה.
- שם קובץ ה-ZIP משקף את הבחירה (חשבוניות_לפי-חודש_... / חשבוניות_לפי-שנה_...).
- שמות הקבצים בפנים ללא שינוי: <תאריך>_<ספק>_<שם הקובץ>.

## קבצים בזיפ (העלה לאותם נתיבים)
1. src/lib/exportZip.ts          — עודכן (פרמטר groupBy: 'month' | 'year').
2. src/App.tsx                   — עודכן (handleExport מקבל groupBy).
3. src/components/GmailConnect.tsx — עודכן (שני כפתורי ייצוא).

## הערה
הייצוא כולל את כל החשבוניות שנמשכו מ-Gmail (עם קובץ מצורף).
אם תרצה בהמשך לייצא רק תקופה מסוימת (למשל רק 2025) לפי הסינון שעל המסך — אפשר להוסיף.
