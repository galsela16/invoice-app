# צפייה בחשבונית בתוך האפליקציה (מוֹדַל) — v0.6.0 → v0.7.0

נבדק מול build אמיתי (tsc -b && vite build) — עובר נקי.

## קבצים בזיפ (העלה ל-GitHub לאותו נתיב)
1. src/components/InvoiceModal.tsx  — חדש. חלון צף שמציג את הקובץ המצורף הראשון.
2. src/components/InvoiceList.tsx   — עודכן. שורה עם קובץ מצורף הפכה ללחיצה + אייקון מהדק.

## מה זה עושה
- לחיצה על חשבונית שיש לה קובץ מצורף פותחת מוֹדַל:
  - PDF מוצג ב-iframe (צפייה מלאה בתוך האפליקציה), תמונה מוצגת כ-img.
  - כותרת: ספק · תאריך · סכום · שם הקובץ.
  - כפתור "הורדה" לקובץ הבודד, וכפתור "סגירה".
  - סגירה גם בלחיצה על הרקע וגם במקש Esc.
- הקובץ נמשך מ-Gmail בזמן הפתיחה ומוצג כ-blob מקומי (ומשוחרר מהזיכרון בסגירה).
- שורה בלי קובץ מצורף לא לחיצה (אין מה להציג).

## שילוב
אין שינוי ב-package.json ואין תלות חדשה — רק שני קבצי רכיב.
Commit → Vercel בונה → לחץ על חשבונית עם 📎.

## הבא בתור (אם תרצה)
- מעבר בין כמה קבצים מצורפים בתוך אותו מוֹדַל.
- עריכת סטטוס/סכום ידנית מתוך המוֹדַל.
