# חילוץ סכום טוב יותר + הסתרת "לא-חשבוניות" — v0.8.0 → v0.9.0

נבדק מול build אמיתי (tsc -b && vite build) — עובר נקי.

## 1) סכומים שלא נקלטו
- נוסף חילוץ מגוף המייל (text/plain, ואם אין אז HTML מנוקה) כנפילה כשה-PDF
  לא נותן סכום. תופס חשבוניות שהסכום שלהן בגוף המייל ולא בקובץ.
- הפרסר הטהור הופרד ל-src/lib/amountParser.ts (בלי pdf.js), כדי שיפעל גם על טקסט מייל.
- נשאר פער אחד: PDF *סרוק* (תמונה בלי טקסט) עדיין לא ייחלץ — צריך OCR (צעד עתידי).

## 2) פריטים שאינם חשבוניות
- סינון אוטומטי שמרני: מייל בלי קובץ מצורף *וגם* בלי סכום שזוהה — לא מוצג
  (ניוזלטרים/פרסומות "חשבונית לחברים" וכו').
- כפתור "הסתר" (✕) בכל שורה — מסתיר ידנית פריט שהוא לא חשבונית.
  ההסתרה נשמרת ב-localStorage ולא חוזרת גם אחרי רענון.

## קבצים בזיפ (העלה לאותם נתיבים)
1. src/lib/amountParser.ts   — חדש (הפרסר הטהור).
2. src/lib/pdfAmount.ts      — עודכן (משתמש ב-amountParser, נשאר עם pdf.js בלבד).
3. src/lib/gmail.ts          — עודכן (חילוץ מגוף המייל + סינון לא-חשבוניות).
4. src/lib/dismissed.ts      — חדש (שמירת מזהים מוסתרים).
5. src/App.tsx               — עודכן (state של מוסתרים + הסרה מהתצוגה).
6. src/components/InvoiceList.tsx — עודכן (כפתור הסתר).

## אימות
גוף עם ₪149 → 149 | גוף עם €8,99 → 8.99 | ניוזלטר → מסונן | חשבונית עם קובץ בלי סכום → עדיין מוצגת (לבדיקה).

## הערה
אם הסינון האוטומטי מסתיר לך חשבונית אמיתית (נדיר: בלי קובץ ובלי סכום שנקרא) —
תגיד לי ונרכך את הכלל.
